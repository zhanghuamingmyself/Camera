<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="CaptureProgressView">
        <attr name="capture_progress" format="float" />
        <attr name="capture_max" format="integer" />

        <attr name="capture_unfinished_color" format="color" />
        <attr name="capture_finished_color" format="color" />

        <attr name="capture_finished_stroke_width" format="dimension" />
        <attr name="capture_unfinished_stroke_width" format="dimension" />

        <attr name="capture_text_size" format="dimension" />
        <attr name="capture_text_color" format="color" />

        <attr name="capture_prefix_text" format="string" />
        <attr name="capture_suffix_text" format="string" />
        <attr name="capture_text" format="string" />

        <attr name="capture_background" format="reference" />

        <attr name="capture_inner_bottom_text" format="string" />
        <attr name="capture_inner_bottom_text_size" format="dimension" />
        <attr name="capture_inner_bottom_text_color" format="color" />

        <attr name="capture_circle_starting_degree" format="integer" />
        <attr name="capture_show_text" format="boolean" />
        <attr name="capture_inner_drawable" format="reference" />
    </declare-styleable>

    <declare-styleable name="RangeSeekBar">
        <!-- the minimum of the range -->
        <attr name="absoluteMinValue" format="integer" />

        <!-- the maximum of the range -->
        <attr name="absoluteMaxValue" format="integer" />
        <attr name="absoluteInterval" format="integer" />

        <!-- the step value -->
        <attr name="step" format="integer|float" />

        <!-- show a single slider -->
        <attr name="singleThumb" format="boolean" />

        <!-- 锁定两个 Thumb 的距离 -->
        <attr name="lockRange" format="boolean" />

        <!-- alwaysActive will make the buttons and bar always draw as the activeColor (the default is false) -->
        <attr name="alwaysActive" format="boolean" />

        <!-- show the currently selected value above each thumb (defaults to true) -->
        <attr name="valuesAboveThumbs" format="boolean" />

        <!-- padding on either side on the slider -->
        <attr name="internalPadding" format="dimension" />

        <!-- the height of the bar -->
        <attr name="barHeight" format="dimension" />

        <!-- the color of the bar that is not selected -->
        <attr name="defaultColor" format="color" />

        <!-- the color of the bar that is selected -->
        <attr name="activeColor" format="color" />

        <!-- the color of the text above thumbs -->
        <attr name="textAboveThumbsColor" format="color" />

        <attr name="rangeBackground" format="reference" />
        <attr name="rangeForeground" format="reference" />

        <!-- set drawables for MIN thumb handles -->
        <attr name="thumbNormal" format="reference" />
        <attr name="thumbPressed" format="reference" />
        <attr name="thumbDisabled" format="reference" />

        <!-- set drawables for MAX thumb handles -->
        <attr name="thumbNormalMax" format="reference" />
        <attr name="thumbPressedMax" format="reference" />
        <attr name="thumbDisabledMax" format="reference" />

        <!-- thumb shadow specifications -->
        <attr name="thumbShadow" format="boolean" />
        <attr name="thumbShadowColor" format="color" />
        <attr name="thumbShadowXOffset" format="dimension" />
        <attr name="thumbShadowYOffset" format="dimension" />
        <attr name="thumbShadowBlur" format="dimension" />

        <!-- to activate the thumbs and texts when the min and max values are the default ones (defaults to false) -->
        <attr name="activateOnDefaultValues" format="boolean" />
    </declare-styleable>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="BDCloudVideoView">
        <attr name="showCacheInfo" format="boolean" />
    </declare-styleable>

</resources>